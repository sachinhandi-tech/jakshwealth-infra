# JakshWealth API Gateway

Terraform for the **jw-api** REST API shell and TOKEN authorizer wiring.

Applied via `code-infra/module/main` (see jakshwealth-infra README).

Lambda functions and route integrations are deployed from **jakshwealth-api**
using modules under `deploy/`.
